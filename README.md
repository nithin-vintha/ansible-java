Install the Java 11 and Java 17 version through ansible

# Step 1
Write the YAML file fro intsall and verify the Java 11 version
   They can use handlers for start the java 11 version afte installation

# step 2
Write a YAML file for Install and verify the Java 17 version 
  It had also use handler to start the Java 17 version after installation
